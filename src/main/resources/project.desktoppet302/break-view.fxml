<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.Label?>
<?import javafx.scene.layout.VBox?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.layout.HBox?>

<?import javafx.scene.layout.StackPane?>

<StackPane xmlns:fx="http://javafx.com/fxml" fx:controller="project.desktoppet302.ButtonController">
    <VBox alignment="CENTER" spacing="30.0" fillWidth="false">
        <HBox spacing="40.00" alignment="CENTER">
            <Button fx:id="pomoBtn"
                    text="Pomodoro"
                    onAction="#pomoBtnClick"
                    disable="false"
            />
            <Button fx:id="scoreBtn" text="Score"/>
        </HBox>

        <HBox spacing="20.0" alignment="CENTER">
            <Label fx:id="timerLabel"/>
        </HBox>

        <HBox spacing="20.0" alignment="CENTER">
            <Button fx:id="startBtn"
                    text="Start"
                    onAction="#startBtnClick"
            />
            <Button fx:id="pauseBtn"
                    text="Pause"
                    onAction="#pauseBtnClick"
            />
            <Button fx:id="resetBtn"
                    text="Reset"
                    onAction ="#resetBtnClick"
            />
        </HBox>
    </VBox>
</StackPane>